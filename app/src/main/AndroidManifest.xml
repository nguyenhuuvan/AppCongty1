<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lv.appcongty1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.InvestmentActivity"></activity>
        <activity android:name=".ui.LoanActivity" />
        <activity android:name=".ui.InsuranceActivity" />
        <activity android:name=".ui.SendGiftSuccessAcivity" />
        <activity android:name=".ui.SendGiftActivity" />
        <activity android:name=".ui.CharityActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.RegisterStep4Activity" />
        <activity android:name=".ui.RegisterStep3Activity" />
        <activity android:name=".ui.RegisterStep2Activity" />
        <activity android:name=".ui.RegisterStep1Activity" />
        <activity android:name=".ui.WecomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>